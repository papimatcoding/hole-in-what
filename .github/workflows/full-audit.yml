name: Full Audit

# Long solver used to certify authored blocks before external beta.
on:
  push:
    branches: [dev]
    paths:
      - "src/data/authored/**"
      - "src/systems/GolfSimulation.ts"
      - "src/systems/CourseValidation.ts"
      - "scripts/courseAudit.ts"
      - ".github/workflows/full-audit.yml"
  workflow_dispatch:

jobs:
  full-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        run: npm install
      - name: Check authored mechanics
        run: npm run test:mechanics
      - name: Check authored geometry
        run: npm run test:geometry
      - name: Check authored clearance
        run: npm run test:clearance
      - name: Full authored campaign audit
        env:
          FULL_AUDIT: "1"
        run: npm run audit:courses
